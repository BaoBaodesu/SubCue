---
description: 防止 SubCue 的模型、构建缓存和发布包重复占用磁盘
alwaysApply: true
---

# 项目体积与发布约束

- 本地 ASR 模型根目录外置，由 `SUBCUE_MODELS_ROOT`、设置项 `modelsRoot` 或 CMake `SUBCUE_MODELS_ROOT` 解析（本机默认 `E:/AIModels/ASR/models`）。仓库内不得再出现权重文件；不得把模型复制进 `models/`、构建树或发布包。
- 模型目录受保护：不得删除、覆盖或纳入通用清理。变更前先记录文件数、总大小和 SHA-256，完成后按 `tools/models-manifest.json` 逐文件复核。下载只允许 `tools/setup-models.ps1`（ModelScope、断点续传、校验、拒绝覆盖已完整模型）。软件内不得自动下载模型。
- 日常开发只保留 `out/build/windows-release-cuda`。`windows-debug` / `windows-release` 仅在无 CUDA 或用户明确不用本地推理时使用，用完即删。`tools/build-windows.bat` 与 `tools/package-windows.bat` 默认 `cuda`。
- 推理运行时在磁盘上最多两份：开发用 `.venv-inference/` 与一份 CUDA 发布包。禁止在多个 `out/build/*/package` 与 `dist/` 同时长期保留完整 Torch 副本；复制并校验成功后删除 staging。
- CUDA 自包含发布必须保留 `cublas64_13.dll`、`cublasLt64_13.dll` 和 `cudart64_13.dll`；不得仅为缩小体积破坏离线运行能力。CPU 包不得携带 Python/Torch。
- 新增依赖、资源或发布文件前，先确认运行时确实引用；禁止把源码仓库、测试素材、CLI、Python 环境、探测产物或未使用的 Qt Quick Controls 风格复制进正式包。新增运行时文件必须出现在 `package-manifest.txt` 并通过 `packageRestrictsPythonAndCliTools`。
- 应用固定使用 `Basic` 风格；除非产品代码明确改用其他风格，否则不得恢复 Material、Fusion、Universal、Imagine、Fluent 或 Windows 风格资源。
- VC Runtime DLL 已完整部署时，不得额外附带 `vc_redist.x64.exe`。
- 临时探测目标、旧构建目录、根目录日志和失败下载残片必须放入已忽略的临时目录，并在任务结束时释放。
- 修改 CMake、打包或依赖后，报告正式包大小。CPU 包目标不超过 610 MiB；CUDA 包目标不超过 4.8 GiB，并说明 Torch 与 cuBLAS 占比。超限必须说明新增内容及必要性。
- 清理只能使用 `tools/clean-workspace.ps1`（默认 `-KeepBuild windows-release-cuda`，预览优先）或应用内 `StorageCleaner`。新增清理项必须解析绝对路径、限制在项目内或应用数据目录，并硬性排除模型根目录、`src/`、`tests/media`、`tests/golden`、`.git/` 与当前构建目录（`package/` 暂存除外）。
- 删除 `.git/lfs/objects` 前必须执行 `git lfs ls-files --all`；存在任意历史引用时保留。不得删除 Git 可达对象或通过改写历史缩小仓库，除非用户明确授权。
- 更新 `.gitignore` 时确保模型、日志、IDE 缓存、临时下载、`CMakeUserPresets.json` 和重复构建产物不会触发 Git LFS clean 或进入版本控制。
