---
description: 约束 SubCue 后台任务、媒体缓存和模型 Worker 的内存生命周期
alwaysApply: true
---

# 内存与资源生命周期约束

- 禁止新增 `.detach()`、失管线程、失管网络请求或捕获裸控制器指针的异步回调；后台任务必须统一持有、可取消、可等待。
- 取消标志必须传递到 Provider、HTTP、下载、校验、解码和 Worker 进程；控制器销毁后不得投递信号、回调或访问成员。
- 关闭软件时先发出取消，再等待任务退出；媒体切换、关闭或失败时必须及时释放 FFmpeg、图像、音频、D3D、网络和临时文件资源。
- 不得用周期性 `gc.collect()`、定时清缓存或无条件强制回收掩盖所有权问题；优先修复生命周期、共享引用和缓存上限。
- 处理长媒体时禁止保存与媒体时长线性增长的中间 PCM、浮点音频、完整帧历史或重复图像；采用流式聚合和固定大小转换缓冲。
- 波形只保留小块转换缓冲及最终金字塔；切换或关闭媒体时，控制器和时间轴必须同时解除旧波形共享引用。
- 逐帧 GOP 缓存与回退历史合计不得超过 128 MiB；优先保留当前帧和最近帧，缓存缺失时按需重新解码。
- 播放包队列、帧队列和音频缓冲必须保持明确的数量或字节上限；新增缓存必须提供可查询的字节统计和超限淘汰逻辑。
- Qwen ASR 与 Forced Aligner 保持顺序装载；阶段切换、任务结束和取消时执行 Python GC 与 CUDA 缓存释放，并终止 Worker、删除临时音频目录。
- 新增后台功能必须有取消/销毁测试，确认退出后无线程、Worker、网络请求、临时目录或延迟回调残留。
- 新增媒体缓存或波形算法必须有边界测试；长媒体临时内存不得随时长增长，帧缓存必须断言不超过 128 MiB。
- 涉及媒体生命周期时保留 50 次打开、切换、播放、关闭回归测试；预热后末段私有内存波动目标不超过 32 MiB。

